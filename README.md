# Wedding scroll-open invitation

Standalone HTML/CSS/JS wedding invitation built from the existing Thu Hương & Văn Lâm content and local assets.

## Run locally

Open `index.html` directly in a modern browser, or serve this folder with any static file server.

The opening scene is intentionally tall: scroll through the first scene to open the two doors and reveal the invitation. No framework or build step is required.
